
class Factory {
    static get(persistence) {
        switch (persistence) {
            case "MEM":
                
                break;
            
            case "FS":
                break;

            default:
                break;
        }
    }
}

export default Factory